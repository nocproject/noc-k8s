---
apiVersion: v1
kind: Service
metadata:
  name: scheduler
  labels:
    service: scheduler
spec:
  ports:
    - name: http
      port: 1200
      protocol: TCP
      targetPort: http
  selector:
    service: scheduler
