---
apiVersion: v1
kind: Service
metadata:
  labels:
    service: scheduler
  name: scheduler
spec:
  ports:
    - name: http
      port: 1200
      protocol: TCP
      targetPort: http
  selector:
    service: scheduler
