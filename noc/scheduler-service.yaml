---
apiVersion: v1
kind: Service
metadata:
  name: scheduler
  labels:
    service: scheduler
spec:
  ports:
  - port: 1200
    protocol: TCP
    name: http
    targetPort: 1200
  selector:
    service: scheduler
