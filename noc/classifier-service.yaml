---
apiVersion: v1
kind: Service
metadata:
  name: classifier
  labels:
    service: classifier
spec:
  ports:
  - port: 1200
    protocol: TCP
    name: http
    targetPort: 1200
  selector:
    service: classifier
