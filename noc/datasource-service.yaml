---
apiVersion: v1
kind: Service
metadata:
  name: datasource
  labels:
    service: datasource
spec:
  ports:
    - name: http
      port: 1200
      protocol: TCP
      targetPort: http
  selector:
    service: datasource
