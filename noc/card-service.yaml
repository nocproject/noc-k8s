---
apiVersion: v1
kind: Service
metadata:
  name: card
  labels:
    service: card
spec:
  ports:
    - port: 1200
      protocol: TCP
      name: http
      targetPort: 1200
  selector:
    service: card
