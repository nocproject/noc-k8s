---
apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    service: web
spec:
  ports:
    - port: 1200
      protocol: TCP
      name: http
      targetPort: 1200
  selector:
    service: web
